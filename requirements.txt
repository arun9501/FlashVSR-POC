# flashvsr_app — app-only dependencies (Python 3.11)
# FlashVSR repo has its own requirements (PyTorch/CUDA, etc.); run inference in that env.

python-dotenv>=1.0.0
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Testing
pytest>=7.0.0
